# Backend Environment Variables Example

NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://backend:backenddbpass@backend-db:5432/mayanconnect

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# External Services
MAYAN_API_URL=http://mayan:8000/api/v4

# Mayan API Token - How to generate:
# 1. Start all containers: docker-compose up -d
# 2. Wait for Mayan to fully start (2-3 minutes)
# 3. Access Mayan web UI: http://localhost:8000
# 4. Login with default credentials: admin / admin
# 5. Go to: Tools → REST API → Token authentication
# 6. Click "Create" to generate a new token
# 7. Copy the token and paste it below
# OR use Django shell:
# docker exec -it mayan-edms python manage.py shell
# from rest_framework.authtoken.models import Token
# from django.contrib.auth import get_user_model
# user = get_user_model().objects.get(username='admin')
# token, created = Token.objects.get_or_create(user=user)
# print(token.key)
MAYAN_API_TOKEN=your_mayan_api_token_here

OLLAMA_API_URL=http://ollama:11434

# CORS
ALLOWED_ORIGINS=http://localhost,http://localhost:80

# Email (Resend)
RESEND_API_KEY=re_your_api_key_here
FROM_EMAIL=noreply@yourdomain.com
FRONTEND_URL=http://localhost
